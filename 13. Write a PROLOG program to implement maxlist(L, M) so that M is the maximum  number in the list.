# Prolog
% The maximum of a single-element list is that element itself.
maxlist([X], X).

% Compare the head with the maximum of the tail.
maxlist([H|T], M) :-
    maxlist(T, M1),
    ( H > M1 -> M = H ; M = M1 ).
# query
?- maxlist([3, 7, 2, 9, 5], M).
?- maxlist([10], M).
?- maxlist([1, 2, 3, 4, 5], M).
?- maxlist([-3, -7, -1, -5], M).
