# Prolog
% memb(X, L) :- X is a member of list L.

% Base case: X is the head of the list
memb(X, [X|_]).

% Recursive case: X is a member of the tail of the list
memb(X, [_|T]) :-
    memb(X, T).
# query
?- memb(3, [1,2,3,4,5]).
?- memb(6, [1,2,3,4,5]).
